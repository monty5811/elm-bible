{
  "name": "elm-bible",
  "version": "1.0.0",
  "license": "BSD3",
  "dependencies": {
    "elm": "^0.18.0",
    "elm-format": "^0.7.0-exp",
    "elm-test": "^0.18.9",
    "elm-verify-examples": "^1.0.2"
  },
  "scripts": {
    "watch": "find . -name '*.elm' | entr -c elm make src/Bible.elm --warn --output /dev/null",
    "watch-test": "find . -name '*.elm' | entr -c yarn test",
    "test": "elm-verify-examples && elm-test --fuzz 1000"
  }
}
